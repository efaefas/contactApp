import 'package:flutter/material.dart';

class MasterScreen extends StatelessWidget {
  static const String routePath = "/master";
  const MasterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
